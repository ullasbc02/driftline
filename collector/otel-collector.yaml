receivers:
  otlp:
    protocols:
      grpc:
      http:

exporters:
  otlphttp:
    endpoint: http://driftline-ingest:9000/ingest/traces
    compression: none

service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [otlphttp]

